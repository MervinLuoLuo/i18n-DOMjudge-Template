{% extends "jury/base.html.twig" %}
{% import "jury/jury_macros.twig" as macros %}

{% block title %}定级比赛 - {{ parent() }}{% endblock %}

{% block extrahead %}
    {{ parent() }}
{% endblock %}

{% block content %}

    <h1>定级比赛</h1>

    {% if blockers is not empty %}
        <div class="alert alert-danger">
            <h5 class="alert-heading">比赛尚不能定级</h5>
            <hr>
            <ul>
                {% for blocker in blockers %}
                    <li>{{ blocker }}</li>
                {% endfor %}
            </ul>
        </div>
    {% else %}
        <div class="col-md-5">
            <table class="table table-sm table-striped">
                <tr>
                    <th>比赛 ID</th>
                    <td>c{{ contest.cid }}</td>
                </tr>
                <tr>
                    <th>比赛名称</th>
                    <td>{{ contest.name }}</td>
                </tr>
                <tr>
                    <th>已开始</th>
                    <td>{{ contest.starttime | printtime }}</td>
                </tr>
                <tr>
                    <th>已结束</th>
                    <td>{{ contest.endtime | printtime }}</td>
                </tr>
            </table>
        </div>

        {{ form_start(form) }}
        {{ form_row(form.b) }}
        {{ form_row(form.finalizecomment) }}
        {{ form_widget(form.finalize) }}
        {{ button(path('jury_contest', {'contestId': contest.cid}), 'Cancel', 'secondary') }}
        {{ form_end(form) }}
    {% endif %}

{% endblock %}
