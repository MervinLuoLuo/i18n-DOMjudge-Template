{{ form_start(form) }}
{{ form_row(form.recipient) }}
{{ form_row(form.subject) }}
{{ form_row(form.message) }}
<div class="mb-3">
    <div class="card">
        <div class="card-body">
            <label class="rendered-preview-badge">渲染预览</label>
            <div id="messagepreview">开始输入以预览消息</div>
        </div>
    </div>
</div>
{{ form_errors(form) }}
<div class="row">
    <div class="col-sm">
        <div class="mb-3">
            <button type="submit" name="submit" class="btn btn-primary"><i class="fas fa-envelope"></i>发送</button>
        </div>
    </div>

    <div class="col-sm text-end">
        {% if answers is defined and answers|length > 0 %}
            <div class="input-group">
                <span class="input-group-text"><i class="fas fa-fish"></i></span>
                <select class="form-select form-control-sm" id="clar_answers">
                    <option selected value="_default">添加预设回复...</option>
                    {% for item in answers %}
                        <option>{{ item }}</option>
                    {% endfor %}
                </select>
            </div>
        {% endif %}
    </div>

</div>
{% if jurymember is defined %}
    <input type="hidden" name="{{ field_name(form.jurymember) }}" value="{{ jurymember }}">
{% endif %}
{{ form_end(form) }}
