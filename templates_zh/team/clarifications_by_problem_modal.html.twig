{% extends "partials/modal.html.twig" %}

{% block title %}查看题目 {{ problem | problemBadgeForContest }} {{ problem.name }} 的质询{% endblock %}

{% block extrahead %}
    {{ parent() }}
    <style>
        .data-table td a, .data-table td a:hover {
            display: block;
            text-decoration: none;
            color: inherit;
            padding: 3px 5px;
        }

        .data-table tr {
            border-bottom: 1px solid silver;
        }

        .data-table tr:hover {
            background: #ffffcc !important;
        }
    </style>
{% endblock %}

{% block content %}
    {% if clarifications is empty %}
        <p class="nodata">暂无质询</p>
    {% else %}
        {% include 'team/partials/clarification_list.html.twig' with {clarifications: clarifications, subject: false} %}
    {% endif %}
{% endblock %}
